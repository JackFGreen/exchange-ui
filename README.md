# exchange-ui
ui 库

## Usage
```sh
# install dep
yarn

# link repo
yarn link

# use in other repo
yarn link exchange-ui

# import all
import ExchangeUi from 'exchange-ui'
Vue.use(ExchangeUi)

# import single
import { ExBtn } from 'exchange-ui'
Vue.component(ExBtn.name, ExBtn)
```
